<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Bootstrap 101 Template</title>


    <link rel='stylesheet' href='/stylesheets/style.css' />

    <!-- Bootstrap -->
    <link href="/lib/bootstrap-3.3.2-dist/css/bootstrap.min.css" rel="stylesheet">

      <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
      <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
      <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
      <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
      <![endif]-->
    <!-- jQuery (necessary for Bootstrap's JavaScript plugins) -->
    <script src="/lib/jquery-1.11.2.min.js"></script>
      <script type="text/javascript" language="javascript">
		$(document).ready(function(){
	      	var select = $('select').val();
	      	$('input[type="text"]').val(select);
	    });
      	
      </script>

</head>

<body>
	<form method="post">
		<h2>Challenge</h2>
		<% if(typeof errors != 'undefined') { %>
		<ul>
			<% errors.forEach(function (error) {%>
			<li><%= JSON.stringify(error) %></li>
			<% }); %>
		</ul>
		<% } %>


		<h1>종목을 선택하세요</h1>

		<br/>

		<select class="form-control">
		  <option name="soccer">축구</option>
		  <option name="baseball">야구</option>
		  <option name="basketball">농구</option>
		  <option name="football">족구</option>
		  <option name="pingpong">탁구</option>
		</select>
		<br />
		<input type="text" name="sport" />
		<br />		
		<input type="submit" value="자동팀원 선택" />
		<input type="submit" value="팀 선택" />
	</form>

	<script>
		$("select").change(function(){
			$('input[type="text"]').val($('select').val());
		});
	</script>

    <!-- Include all compiled plugins (below), or include individual files as needed -->
    <script src="/lib/bootstrap-3.3.2-dist/js/bootstrap.min.js"></script>
</body>
</html>